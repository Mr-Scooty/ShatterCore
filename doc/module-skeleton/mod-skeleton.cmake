# Optional per-module CMake hook. This file is include()d during the
# configuration of modules/CMakeLists.txt if it exists, so a module can add
# extra dependencies, definitions or sources. Most modules don't need it.
#
# Example:
# message(STATUS "mod-skeleton: custom cmake hook executed")
